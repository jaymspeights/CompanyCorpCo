<link rel="stylesheet" href="/css/landing.css">
<script type="text/javascript">
	function getloc(){
		navigator.geolocation.getCurrentPosition(test);
	};
	let test = (pos)=>{
		let coords = pos.coords;
		$.get("req/reps?latlng="+coords.latitude+","+coords.longitude, (data)=>{
			document.body.innerHTML = data;
		});

	};
  function goto(name){
    console.log("idk");
    $.get("/congress?name="+name, (data)=>{
			console.log("goteem");
		});

  }
	// getloc();
	function findCookie(name){
		s = document.cookie.split(";");
		for(let i = 0; i < s.length; i++){
			e = s[i].split("=");
			if(e[0] == name){
				return e[1];
			}
		}
		return undefined;
	}
	if(!findCookie("id")){
		$.get("/req/id",(data)=>{
			document.cookie = "id="+data.id
		})
	}
</script>

<h1>Find your Representative</h1>
<input type="button" onclick="getloc()" value="Find by Location">
